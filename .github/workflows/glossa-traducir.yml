name: Glossa traducir (el número en español)

# Aparte del que escribe el número, y esa separación se ganó midiéndola:
# traducir es otra llamada de ~14 min a una cuenta que admite UNA petición a la
# vez. Colgada del guion que escribe, convirtió treinta minutos en cincuenta y
# dejó esperando la publicación de un número ya terminado.
#
# Domingo 10:45 UTC (03:45 LA en verano), con el número ya escrito y el
# consejo terminado — la cuenta de Kimi admite una petición a la vez y el
# revisor la usa. Si falla, el inglés ya está publicado; el conductor de la
# base (0063) reintenta por condición, este cron es la red.
#
# Traduce con una CASCADA verificada (scripts/revisor_es.mjs): Gemini (gratis)
# → Grok sin razonamiento → Haiku. Cada intento pasa por el fusible entero —que
# en español incluye el contrato de src/lib/espanol.js: calcos, campos en
# inglés, cifras, paridad— y un fallo grave se le DICE al mismo modelo antes de
# pasar al siguiente. Cuando el determinista aprueba, Kimi dictamina como
# corrector de estilo (centavos); si dice «corrige», una última vuelta.
#
# Secretos: SUPABASE_SERVICE_KEY, GEMINI_API_KEY, XAI_API_KEY, ANTHROPIC_API_KEY,
#           MOONSHOT_API_KEY (el revisor; sin él se publica con solo el determinista).

# ── El reloj de la semana ────────────────────────────────────────────────
# Todo tiene que estar TERMINADO antes de las 5:00 de la mañana en Los Ángeles,
# y con margen: una corrida de Kimi que se ralentice puede pasar de 16 minutos a
# 50, y eso ya ocurrió. La cadena, en hora de allí:
#
#   sábado  08:00  reportaje — sale a buscar fuera (cubre ya casi la semana)
#   sábado  21:00  cotejo    — comprueba afirmaciones contra documentos
#   domingo 01:30  número    — la semana cerró a medianoche; se escribe
#   domingo 03:00  consejo   — mide la calibración; usa la cuenta de Kimi
#   domingo 03:45  español   — la edición española, con su revisor Kimi, al final
#
# Termina sobre las 04:15 y queda colchón hasta las 05:00. Consejo y español van
# DESPUÉS del número a propósito: los tres usan la misma cuenta de Kimi, que
# admite una petición a la vez. Estos crons son la red: el conductor de la base
# (0063) empuja por condición y suele llegar antes.

on:
  schedule:
    - cron: '45 10 * * 0'
  workflow_dispatch:
    inputs:
      semana:
        description: 'Qué semana traducir (YYYY-MM-DD). Vacío = la última sin traducir'
        required: false

permissions:
  contents: read

concurrency:
  group: glossa-traducir
  cancel-in-progress: false

env:
  SUPABASE_URL: https://wtwuvrtmadnlezkbesqp.supabase.co
  SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
  GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
  XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
  ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
  MOONSHOT_API_KEY: ${{ secrets.MOONSHOT_API_KEY }}
  SEMANA: ${{ inputs.semana }}

jobs:
  traducir:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
      - run: node scripts/traducir_from_supabase.mjs

      # La tarjeta de compartir, con el titular ya escrito. Si falla no tumba
      # nada: un enlace sin imagen es peor que uno con imagen, y muchísimo mejor
      # que perder lo que se acaba de escribir.
      - name: Draw the share cards
        if: success()
        continue-on-error: true
        run: |
          npm ci --no-audit --no-fund
          node scripts/tarjetas.mjs

